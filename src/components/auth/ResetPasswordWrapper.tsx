import { ResetPasswordRequestForm } from "./ResetPasswordRequestForm";

/**
 * Simple wrapper for password reset request form
 * Used on /reset-password page
 */
export function ResetPasswordWrapper() {
  return <ResetPasswordRequestForm />;
}
